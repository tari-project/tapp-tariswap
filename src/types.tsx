export type JoinExitPoolInput = {
  firstTokenAmount: string
  secondTokenAmount: string
}

export type SwapInput = {
  InTokenSymbol: "A" | "B"
  firstTokenAmount: string
  secondTokenAmount: string
}
